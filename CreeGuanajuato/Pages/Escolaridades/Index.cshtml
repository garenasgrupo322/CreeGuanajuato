@page
@model CreeGuanajuato.Pages.Escolaridades.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Escolaridad[0].nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Escolaridad) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id_escolaridad">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id_escolaridad">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id_escolaridad">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
