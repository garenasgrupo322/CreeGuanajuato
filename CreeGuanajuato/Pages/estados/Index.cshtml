@page
@model CreeGuanajuato.Pages.Estados.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Estado[0].nombre_estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Estado) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre_estado)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id_estado">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id_estado">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id_estado">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
