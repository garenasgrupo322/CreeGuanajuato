@page
@model CreeGuanajuato.Pages.Direcciones.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h2>Direcciones</h2>

<p>
    <a asp-page="Create">Nuevo registro</a>
</p>

<form asp-page="./Index" method="get">
    <div class="row">
        <div class="col-lg-offset-5 col-md-1">
            <label>Búsqueda</label>
        </div>
        <div class="col-md-4">
            <input type="text" name="busqueda" value="@Model.CurrentFilter" class="form-control" />
        </div>
        <div class="col-md-1">
            <input type="submit" value="Búsqueda" class="btn btn-default" />
        </div>
        <div class="col-md-1">
            <a asp-page="./Index" class="btn btn-default">Limpiar</a>
        </div>
    </div>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Direccion[0].calle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Direccion[0].numero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Direccion[0].Colonia)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Direccion)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.calle)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.numero)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Colonia.nombre_colonia)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.id_direccion">Modificar</a> |
                    <a asp-page="./Details" asp-route-id="@item.id_direccion">Detalles</a> |
                    <a asp-page="./Delete" asp-route-id="@item.id_direccion">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@{
    var prevDisabled = !Model.Direccion.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.Direccion.HasNextPage ? "disabled" : "";
}

<a asp-page="./Index"
   asp-route-sortOrder="@Model.CurrentSort"
   asp-route-pageIndex="@(Model.Direccion.PageIndex - 1)"
   asp-route-currentFilter="@Model.CurrentFilter"
   class="btn btn-default @prevDisabled">
    Anterior
</a>
<a asp-page="./Index"
   asp-route-sortOrder="@Model.CurrentSort"
   asp-route-pageIndex="@(Model.Direccion.PageIndex + 1)"
   asp-route-currentFilter="@Model.CurrentFilter"
   class="btn btn-default @nextDisabled">
    Siguiente
</a>